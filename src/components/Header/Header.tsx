import Styles from "./Header.module.scss";
function Header() {
  return (
    <header className={Styles.header}>
      <nav>
        <h1>Ticketmaster.</h1>
      </nav>
    </header>
  );
}

export default Header;


  